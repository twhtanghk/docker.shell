version: '2'
services:
  mysql:
    image: mariadb
    container_name: mysql
    hostname: mysql
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
    ports:
      - 3306
    volumes:
      - ${COMPOSEROOT}/mysql/conf/my.cnf:/etc/mysql/my.cnf
