version: '2'
services:
  oauth2:
    image: twhtanghk/oauth2
    container_name: oauth2
    hostname: xrdp
    ports:
      - 8000
    volumes:
      - ${COMPOSEROOT}/oauth2/conf/env.py:/usr/src/app/organization/env.py
      - ${COMPOSEROOT}/oauth2/log:/usr/src/app/log