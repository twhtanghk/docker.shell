version: '2'
services:
  gnuradio:
    image: marcelmaatkamp/pybombs-gnuradio-rtlsdr
    privileged: true
    volumes:
      - /dev:/dev
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=:0.0
    entrypoint: top -b -d 1000
